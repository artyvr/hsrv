[Unit]
Description=FileBrowser https://filebrowser.org/ docker compose service
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/bash -c "docker compose -f /opt/hsrv/docker-compose-filebrowser.yml up --detach"
ExecStop=/bin/bash -c "docker compose -f /opt/hsrv/docker/docker-compose-filebrowser.yml stop"

[Install]
WantedBy=multi-user.target
